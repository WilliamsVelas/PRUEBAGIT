import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'peliculasApp2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
